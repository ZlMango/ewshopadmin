import request from '@/utils/request'


// 封装获取阿里云OSSToken，用于前端直传文件接口的请求
export function uploadToken(){
	return request({
		url:'/api/auth/oss/token'
	})
}









































































